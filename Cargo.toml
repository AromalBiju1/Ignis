[package]
name = "ignis"
version = "0.1.0"
edition = "2021"

[dependencies]
uefi = { version = "0.33", features = ["panic_handler", "logger","alloc","global_allocator"]}
log = "0.4"

[profile.release]
panic = "abort"
lto = true


